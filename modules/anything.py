"""Модуль, содержащий что-угодго."""


class Anything:
    """Класс чего-угодно. Что-угодно равно чему-угодно (удивительно, не так ли?)
    """

    # Необходимо было для инкостыляции приложения, более не используется
    # (наверное)
    def __eq__(self, other):
        return True

    def __hash__(self):
        return 0


anything = Anything()
